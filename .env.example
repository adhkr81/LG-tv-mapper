# Serial port configuration
SERIAL_PORT=COM3
SERIAL_BAUD=115200

# Path where USB screenshots land on your PC (optional - for watch mode)
USB_WATCH_PATH=

# Server port
PORT=3001

# Emulator config (written on every save — no export step)
# EMULATOR_DATA_PATH=../context/emulator.json
# MAPPER_META_PATH=./server/src/data/mapper-meta.json
